/*
 * blowfishopt.c
 *
 * An empty file, in case the library is compiled on a platform for which there are no
 * assembler-optimized functions implemented (yet)
 *
 * And just so that the compiler doesn't complain:
 */

#define BEECRYPT_DLL_EXPORT

#include "blowfishopt.h"
